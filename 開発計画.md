# ジャグラー実戦記録ツール 開発計画

最終更新: 2026-08-11

## 1. プロジェクト概要

パチスロ「ジャグラー」シリーズの実戦データを記録・分析するための個人用ツール。
ホールで打ちながらスマホで使うことを最優先とする。

**解決したい課題**
- 開始時のデータカウンターを記録し、自分の実績を差分で正確に把握したい
- 投資/回収と収支を、その場で手間なく記録したい
- ぶどう確率などから設定を推測し、やめ時の判断材料にしたい
- 過去の収支を振り返って自分の立ち回りを評価したい

## 2. 確定事項

| 項目 | 決定 |
|---|---|
| 形態 | PWA(スマホWebアプリ / ホーム画面に追加して使用) |
| 対応機種 | 主要ジャグラー全機種 |
| スコープ | 収支・カウンター記録 / 実戦カウンター画面 / 設定推測(ベイズ判別) / 統計ダッシュボード の全4機能 |
| データ保管 | 端末内(IndexedDB)。アカウント登録なし |
| 初期スコープ外 | クラウド同期・ログイン・他ユーザーとの共有 |
| **コスト制約** | **Claude Code 以外の課金は一切しない(追加費用0円で完結させる)** |

## 3. 技術構成

```
React 18 + TypeScript
Vite                … ビルド
Tailwind CSS        … 片手操作UI / ダークモード
Dexie (IndexedDB)   … 端末内DB・オフライン動作
Recharts            … 収支グラフ
vite-plugin-pwa     … インストール & オフラインキャッシュ
```

デプロイ先: GitHub Pages(パブリックリポジトリ / 完全無料・クレジットカード登録不要)

**この構成を選ぶ理由**
- ホールは電波が弱いことがあるため、オフラインで完結する必要がある
- アプリストア審査を通さずに自分だけで更新できる
- データが端末内に閉じるため、金額情報を外部に出さずに済む

## 4. コスト制約(追加課金なしで完結させる)

**Claude Code 以外の課金は行わない。** 以下の方針で追加費用0円を担保する。

| 項目 | 採用するもの | 費用 |
|---|---|---|
| ライブラリ | React / Vite / Tailwind / Dexie / Recharts(すべてOSS・MITライセンス) | 0円 |
| ホスティング | GitHub Pages(パブリックリポジトリ) | 0円 |
| ドメイン | `github.io` のサブドメインをそのまま使用(独自ドメインは購入しない) | 0円 |
| スマホへの導入 | Safari/Chrome の「ホーム画面に追加」 | 0円 |
| データ保管 | 端末内 IndexedDB(クラウドDBを使わない) | 0円 |
| 外部API | 使用しない(機種データは自前マスタ、設定推測もローカル計算) | 0円 |

**この制約から自動的に決まること**
- **iOSアプリ化はしない**。App Store 配信には Apple Developer Program(年間約12,800円)が必要なため、
  PWAの「ホーム画面に追加」で代替する。動作・見た目はネイティブアプリとほぼ同等になる。
- **プライベートリポジトリは使わない**。GitHub Pages を無料アカウントのプライベートリポジトリから
  公開することはできないため、パブリックリポジトリを使用する。
  → **収支データはリポジトリに一切含まれない**(すべて端末内 IndexedDB に保存される)ため、
     公開されるのはソースコードのみであり、金額情報が外部に出ることはない。
- **クラウド同期・バックアップサービスは使わない**。機種変更時のデータ移行は CSVエクスポート/インポートで行う(Phase 5)。
- **有料の設定判別データベースやAPIは使わない**。機種マスタは自前で整備する(Phase 0)。

**将来 GitHub すら使いたくない場合の代替案(同じく0円)**
ビルド成果物を PC でローカルサーバーとして起動し、同一Wi-Fi内のスマホから開く方法も可能。
ただし HTTPS でないため PWA のインストールとオフラインキャッシュに制限が出るので、GitHub Pages を推奨する。

## 5. 非機能要件(ホールで使うための条件)

- **オフラインで全機能が動作すること**(通信必須の処理を作らない)
- **ダークモード既定**(ホール内は暗く、画面が明るいと目立つ)
- **片手・親指操作**:主要ボタンは画面下半分に配置、タップ領域は最低 56px
- **誤タップ対策**:カウント操作は必ず1手で取り消せること
- **中断耐性**:入力途中で画面を閉じても内容が消えないこと(逐次保存)
- データ破損に備え、CSVエクスポートでバックアップを取れること

## 6. データモデル

```ts
// 実戦セッション(1台=1レコード)
type Session = {
  id: string
  date: string            // YYYY-MM-DD
  hall: string            // 店名
  machineNo: string       // 台番号
  machineTypeId: string   // 機種マスタへの参照
  start: CounterSnapshot  // 着席時のデータカウンター(前任者ぶんを含む)
  end: CounterSnapshot | null   // 離席時のデータカウンター
  myCount: {              // 自分が打った実績(実戦カウンター画面で計上)
    games: number
    bb: number
    rb: number
    grape: number
    soloRb: number        // 単独REG。機種によってはぶどうより判別力が高い
  }
  invest: number          // 投資額(円)
  payoutMedals: number    // 回収枚数
  exchangeRate: number    // 交換率(1枚あたりの円)
  startedAt: string       // ISO8601 実働時間の算出用
  endedAt: string | null
  memo: string
}

type CounterSnapshot = {
  games: number     // 総回転数
  bb: number
  rb: number
  medals: number    // 差枚数
}

// 機種マスタ(設定1〜6の理論値)
type MachineSpec = {
  id: string
  name: string            // 例: マイジャグラーV
  settings: SettingSpec[] // 6件
}

type SettingSpec = {
  setting: 1|2|3|4|5|6
  bbProb: number          // 1/x の x
  rbProb: number
  grapeProb: number
  payoutRate: number      // 出玉率(%)
}
```

**導出値(保存せず計算で求める)**
- 自分の実績G数 = `end.games - start.games`
- 収支 = `payoutMedals * exchangeRate - invest`
- 時給 = `収支 / 実働時間`
- 合算確率 = `games / (bb + rb)`

## 7. 画面構成

| 画面 | 役割 |
|---|---|
| ホーム | 今日の収支サマリ、進行中セッションへの復帰、「新規実戦」ボタン |
| 実戦開始 | 店名・台番号・機種・開始時カウンターの入力 |
| **実戦カウンター** | 打ちながら使うメイン画面。ぶどう/BB/RBのタップ計上、リアルタイム確率、投資の加算 |
| 設定推測 | 各設定の期待度%、判別根拠(ぶどう/合算)、やめ時サジェスト |
| 実戦終了 | 終了時カウンター・回収枚数の入力、収支確定 |
| 履歴一覧 | セッション一覧、絞り込み、編集・削除 |
| 統計 | 月別収支グラフ、累計差枚推移、勝率・時給、機種別/店舗別/曜日別集計 |
| 設定 | 交換率、既定の店名、機種マスタ編集、CSV入出力 |

## 8. 設定推測アルゴリズム

ベイズ推定により、観測データから各設定の事後確率を求める。

1. 事前分布は設定1〜6を一様(各1/6)とする。ホールの傾向を後から反映できるよう設定画面で調整可能にする
2. 尤度は各事象を独立とみなして計算する
   - ぶどう回数 / 単独REG回数 ~ 二項分布(試行数 = 自分のG数、確率 = 設定別の理論値)
   - BB回数 / RB回数 ~ 二項分布(同上)
3. 事後確率 ∝ 事前確率 × 尤度、正規化して%表示する
4. どの指標を主軸として見せるかは機種ごとに異なるため、**機種マスタの数値から自動判定する**(§8.1)
5. サンプル数が少ないうちは判別が無意味なので、**信頼度インジケータ**(例: 3000G未満は「参考値」表示)を必ず添える

### 8.1 「何を数えるべきか」の判定 ✅ Phase 0 で実装済み

当初の「ジャグラーはぶどうを主指標にする」という前提は **全機種には通用しない** ことが Phase 0 の調査で判明した。

**(a) ぶどうの設定差構造が機種ごとに違う**

| 構造 | 該当機種 | 内容 |
|---|---|---|
| 全設定に差 | マイジャグラーV / ゴーゴージャグラー3 / ファンキージャグラー2 / ハッピージャグラーVⅢ / ミスタージャグラー | 設定1〜6がすべて別値 |
| 設定1〜4が同値 | ウルトラミラクルジャグラー / ジャグラーガールズSS | 差が付くのは設定5・6のみ |
| 設定1〜5が同値 | アイムジャグラーEX / ネオアイムジャグラーEX | ぶどうは「設定6か否か」しか判別できない |

**(b) 判別力を「設定1と6で何倍違うか」で測ってはいけない**

ぶどうは約1/6で出るため1000Gで約170回の試行が得られるが、REGは約1/300なので3回程度しか得られない。
相対差の大きさだけを見ると、試行数が桁違いに少ない指標を過大評価してしまう。
**設定間のKLダイバージェンス(1Gあたりに得られる情報量)** で比較する。
全設定ペアの平均を取ることで、「設定1〜5が同値=その範囲の情報量ゼロ」が正しく低評価される。

**(c) データカウンターから無料で得られる指標と、手カウントが必要な指標を混ぜない**

REG確率・合算確率は台のデータカウンターから自動で得られるため、数える労力はゼロ。
プレイヤーが労力を割く必要があるのは **ぶどう** と **単独REG** の2つだけなので、
「何を数えるべきか」はこの2つに絞って比較しなければ答えにならない。

**判定結果**(`recommendedManualCount()` が自動算出)

| 機種 | 数えるべき指標 | 情報量の比 |
|---|---|---|
| マイジャグラーV | 単独REG | ぶどうの約4.2倍 |
| ネオアイムジャグラーEX / アイムジャグラーEX | 単独REG | 約2.0倍 |
| ウルトラミラクルジャグラー | 単独REG | 約4.1倍 |
| ジャグラーガールズSS | 単独REG | 約1.9倍 |
| ファンキージャグラー2 | 単独REG | 約1.6倍 |
| **ゴーゴージャグラー3** | **ぶどう** | 単独REGの約1.2倍 |
| ハッピージャグラーVⅢ / ミスタージャグラー | ぶどう | 単独REGの解析値が存在しないため |

この結果は、調査時に各解析サイトが述べていた見解(「マイジャグは単独REG > ぶどう」「ゴーゴー3はぶどうが機能する」
「ウルミラはぶどうを主指標にできない」)とすべて一致した。ユニットテストで固定してある。

### ⚠ ぶどう確率は「解析値」であり公表値ではない

北電子は小役確率を公表していないため、流通しているぶどう確率はすべて第三者の解析値である。
機種によっては複数系統の数値が流通しており、どれを採用するかで尤度計算の結果が変わる。

| 機種 | 状況 |
|---|---|
| ハッピージャグラーVⅢ | 解析値が**4系統**に分裂(設定6で最大0.07差)。最も不確実 |
| マイジャグラーV | 設定6が 1/5.69派(4情報源) vs 1/5.66派(5情報源)で**4対5と拮抗し決着不能** |
| ミスタージャグラー | 2系統が全設定で一律に約0.15ずれる。刻み幅は一致するため集計基準の違いと思われる |
| ゴーゴージャグラー3 | 3系統が流通(最大0.08差) |

対応方針:
- 採用値と対立系統の両方を `phase0-research/*.json` の `conflicts` に保存済み
- 対立系統は機種マスタの `grapeProbAlt` に持たせ、Phase 3 で感度分析に使う
- 複数系統が存在する機種は `dataStatus: 'provisional'` とし、UIに「暫定値」を表示する
- 数千G規模の単一ホール実戦値は理論値ではないため採用しない

**なお BB / RB / 合算 / 出玉率 は全機種・全情報源で完全一致しており、実質メーカー公表値とみなせる。**
食い違いはすべて小役(ぶどう・単独REG)に集中している。

## 9. 開発工程

各フェーズの終了時点で必ず「動くもの」が残る形で進める。
Phase 1 完了時点で一度実戦投入し、その使用感を Phase 2 以降に反映する。

### Phase 0 — 準備・機種マスタ整備 ✅ 完了(2026-08-11)
- [x] Vite + React + TS プロジェクト作成、Tailwind / Dexie 導入
- [x] 現行ジャグラー全9機種の設定1〜6理論値テーブルを作成
      マイジャグラーV / ネオアイムジャグラーEX / ゴーゴージャグラー3 / ファンキージャグラー2 /
      アイムジャグラーEX / ハッピージャグラーVⅢ / ウルトラミラクルジャグラー /
      ミスタージャグラー / ジャグラーガールズSS
- [x] 数値の出典確認(機種ごとに3情報源以上を突き合わせ、食い違いは両論併記で保存)
- [x] IndexedDB スキーマ定義(Dexie v1)
- [x] 判別指標の自動判定ロジック(§8.1)とユニットテスト70件

**Phase 0 で追加になった作業**(当初計画に無かったもの)
- 機種マスタを調査JSONから生成する `scripts/generate-machines.py` を作成。
  手で数値を書き写すと転記ミスが起きるため、調査結果を唯一の情報源とした。
  新機種の追加は JSONを置いてスクリプトを再実行するだけで済む
- データモデルに `soloRbProb`(単独REG) / `grapeProbAlt`(対立解析値) を追加
- 「何を数えるべきか」の自動判定(§8.1)。当初はぶどう固定の想定だった

### Phase 1 — MVP: 記録できる状態にする ✅ 完了(2026-08-11)
- [x] 実戦開始フォーム(日付・店名・台番号・機種・着席時カウンター)
- [x] 実戦終了フォーム(離席時カウンター・回収枚数・メモ)
- [x] 収支計算ロジック(交換率6種対応、投資/回収/収支/実働時間/時給)
- [x] セッション履歴一覧(日付ごと・累計収支・勝率)、詳細・編集・削除
- [x] 逐次保存(入力のたびにIndexedDBへ書き込み、途中で閉じても消えない)
- [x] ホーム画面(今日の収支・実戦中セッションへの復帰)
- [x] 設定画面(交換率・既定の店名/機種)
- [x] テスト112件(計算 / DB操作 / 画面描画)
- ⏳ **実戦投入して使用感を確認する** ← 次はここ

**画面構成**
| ルート | 画面 |
|---|---|
| `#/` | ホーム。今日の収支、実戦中への復帰、新規実戦 |
| `#/start/:id` | 実戦開始。店名・台番号・機種・着席時カウンター |
| `#/play/:id` | 実戦中。投資の加算、経過時間(Phase 2 でカウンターを追加) |
| `#/end/:id` | 実戦終了。離席時カウンター・回収枚数・収支確定 |
| `#/history` | 履歴。日付ごとの一覧、累計収支・勝率 |
| `#/session/:id` | 詳細・編集・削除 |
| `#/settings` | 設定 |

**Phase 1 で判明した実装上の判断**
- 「保存ボタンを押したら書き込む」ではなく、**新規実戦を開いた時点で空レコードを作り、入力のたびに書き込む**方式にした。
  これで中断耐性の要件を確実に満たせる。ただし空レコードが履歴に残らないよう、
  何も入力せずに戻った場合はレコードごと削除する
- ルーティングはライブラリを使わず**ハッシュベースの自作**(約30行)。
  静的ホスティングでリロードしても404にならず、スマホの戻るボタンもそのまま効く
- 実戦開始時に店名・機種を設定の既定値として自動保存し、次回の入力を省略できるようにした

### Phase 2 — 実戦カウンター画面 ✅ 完了(2026-08-11)
- [x] 大型タップボタン(ぶどう / BB / 単独REG / チェリーREG / 不明REG / 取り消し)
- [x] 単独REGのカウント。**「分類できなかったREG」の逃げ道も用意**(後述)
- [x] 現在G数の随時更新入力(下書き方式)
- [x] リアルタイム確率表示(合算/BB/REG/ぶどう/単独REG)と設定1〜6のメーター
- [x] 投資の +1000円 ボタン
- [x] ダークモード・片手レイアウトの作り込み
- [x] テスト164件
- ⏳ **実戦投入して使用感を確認する** ← 次はここ

**計画から意図的に変えた点**
- 「推奨指標が単独REGのときは主ボタンとして大きく配置する」は**採用しなかった**。
  単独REGは1/500程度しか押さないのに対し、ぶどうは1000Gで約170回押す。
  ボタンの大きさと位置は押下頻度で決めるべきなので、最下段の全幅は常にぶどうに割り当て、
  推奨は文字バッジで示す
- 指標を色で区別する案は**破棄した**。BB赤/RB青/ぶどう紫を検証したところ、
  青と紫が赤緑色覚で判別不能(ΔE 0.9、基準は8.0)。5パターン試して全滅した。
  各行にラベルがあり色に識別を担わせる必要がないため、文字トークン＋単一アクセントに変更

**レビューで見つけて直した不具合(8件)**
| 内容 | 影響 |
|---|---|
| `myCount` を丸ごと書き戻す経路が残っていた | カウント中にG数を打つと直前のタップが黙って巻き戻る |
| 単独REGの「0回」と「未計測」が区別不能 | 強い低設定の証拠と、証拠なしが同じデータになる |
| 取り消し履歴が実データとずれる | 手動修正後の取り消しで二重に減る |
| `click` 依存 + `touch-action` 未指定 | iOSで約300msの遅延。指が滑るとタップが消える |
| メーターが存在しない設定を指す | 同値の設定がある機種で連続目盛りとして描画していた |
| 「取り消し」が最も押しやすい位置 | 連打中に親指が流れると確認なしで1回消える |
| iOSに振動APIが無い | 押せたか確認する手段がゼロ |
| 総回転数入力がフォーカス時に全選択→即書き込み | 打ちかけの中間値でG数が0に潰れる |

### ⚠ ぶどうの分母は「通常時G数」でなければならない(Phase 2 調査で判明)

データカウンターの総回転数にはボーナス消化中のゲームも含まれる。
ぶどうは通常時にしか数えないため、総回転数で割ると**ぶどう確率が約1割悪く出て、
判別が丸ごと低設定側にずれる**。

`通常時G数 = 総回転数 − (BB回数 × BIG平均消化G + RB回数 × REG平均消化G)`

**平均消化ゲーム数は定数として確定できる。**
6号機ジャグラーはボーナス中のみ2枚掛けになり、ぶどうがフリー打ちで100%揃って
毎ゲーム14枚払い出される。確率的なブレが無いため消化G数は固定値になる。

| 機種 | BIG | REG | 検算(純増 = 消化G × (14−2)) |
|---|---|---|---|
| アイムジャグラーEX / ネオアイムジャグラーEX | 21G | 8G | 252枚 / 96枚 ✓公表値一致 |
| その他7機種 | 20G | 8G | 240枚 / 96枚 ✓公表値一致 |

※ 5号機の感覚(3枚掛け・8枚役)で 240÷8=30G と計算すると3割ずれる。

**その他の実務ルール**
- ぶどうは**通常時かつ3枚掛けのみ**数える。1枚掛け(ブドウ抜き)は6号機ジャグラー全般で対象外
- ペカ後にボーナスを揃えるまでのゲームは**カウント対象**。
  境界は告知の有無ではなく「3枚掛けか」「ボーナス作動中か」
- 単独REGの判定には**左リールのチェリー狙いが必須**。
  先ペカでそのまま揃えると契機が永久に不明になるため、「不明REG」の受け皿が要る
  (誤って単独/チェリーに寄せると、1回の誤りで期待値3〜4回のデータが25〜33%狂う)

### Phase 3 — 設定推測エンジン ✅ 完了(2026-08-11)
- [x] ベイズ推定ロジック実装(§8)
- [x] 設定1〜6の期待度%表示 + 信頼度インジケータ
- [x] 判別根拠の内訳表示(ボーナス / ぶどう / 単独REG の各寄与)
- [x] **ぶどう解析値の感度分析**。grapeProbAlt の系統でも計算し、結論が変わる場合は警告を出す
- [x] 単独REGの尤度は**不明REGを除いた既知契機のみ**を母数にする
- [x] ぶどうの尤度は**通常時G数**を母数にする(総回転数と混ぜない)
- [x] やめ時サジェスト(基準は設定画面で変更可)
- [x] ユニットテスト22件
- [x] 使い方に「設定推測の見かた」を追記

**尤度の組み立て方**
| 指標 | 分布 | 母数 |
|---|---|---|
| ボーナス | BB / RB / どちらでもない の**多項分布** | 自分のG数 |
| ぶどう | 二項分布 | **通常時G数**(ボーナス消化ぶんを除く) |
| 単独REG | 「REGを引いたとき単独である」条件付き確率の二項分布 | **分類できたREGの回数** |

- BBとRBを別々の二項分布にすると「どちらも引かなかったゲーム」を二重に数えるため、多項分布にした
- 単独REGを条件付きで扱うことで、契機不明のREGを自然に除外できる。
  REGを引いたこと自体はボーナスの項で数えているので二重計上にもならない

**「判断材料が乏しい」の基準**
設定6の理論値どおりに当たり続けた場合で実測すると、事後確率の最大値は
300Gで18%、1000Gで20%、2000Gで31%、1万Gで64%と動く(一様分布は16.7%)。
どの設定も25%に届かないうちは「乏しい」と表示する。実感に近い2000G前後で表示が外れる。

**あえて実装しなかったもの**
事前分布の調整(店ごとの高設定の入りやすさ)。適当な値を入れると結論を歪めるだけなので、
一様分布のまま「実際の店はこれより低く見積もるべき」と画面に明記する方針にした。

### Phase 4 — 統計ダッシュボード(1日)
- [ ] 月別収支の棒グラフ、累計差枚の折れ線グラフ
- [ ] 勝率・平均時給・総投資/総回収のサマリ
- [ ] 機種別 / 店舗別 / 曜日別の集計
- [ ] 期間フィルタ

### Phase 5 — 仕上げ ✅ 大半を前倒しで完了(2026-08-11)

Phase 2 は実戦で触らないと評価できないが、`npm run dev:host` はPCを起動したまま
同じWi-Fiにいる必要があり、ホールでは使えない。そのためデプロイを先に済ませた。

- [x] PWA化(manifest / Service Worker / ホーム画面追加 / オフライン動作)
- [x] CSVエクスポート・インポート
- [x] デプロイ設定(GitHub Pages + GitHub Actions、追加費用なし)
- [x] ストレージ永続化の要求と状態表示
- [ ] 設定画面の残り(機種マスタ編集・推測パラメータ)← Phase 3 と合わせて実施

**iOS Safari のデータ削除対策**
iOS Safari は7日間アクセスの無いサイトのIndexedDBを削除する。収支の記録が消えるのは
致命的なので、次の3段構えにした。
1. `navigator.storage.persist()` で永続化を要求する(通る保証はない)
2. ホーム画面に追加していない場合、設定画面に警告を出す
3. CSVバックアップを用意する(これが唯一確実な手段)

**公開範囲**
無料アカウントで github.io を使うにはパブリックリポジトリが必須。
公開されるのはソースコードのみで、収支データは端末内のIndexedDBにしか存在しない。

## 10. 要検証事項 / リスク

### 解消済み(Phase 0)
- ~~機種マスタの数値精度~~ → 機種ごとに3情報源以上で照合済み。
  BB/RB/合算/出玉率は全情報源が完全一致。食い違いは小役に限定され、対立系統も保存済み
- ~~現行機種の把握~~ → 9機種を確認。当初リストから漏れていたネオアイムジャグラーEX(設置2位)を追加

### 継続する課題
- **ぶどう解析値の系統差**が尤度計算に影響する。特にハッピージャグラーVⅢ(4系統)とマイジャグラーV(4対5で拮抗)。
  Phase 3 で感度分析を実装し、系統によって結論が変わる場合はUIに警告を出す
- **ぶどう・単独REGは手カウントのため、カウント漏れが判別精度に直結する。**
  取り消し操作は必須。カウント漏れを自己申告して補正する機能の要否を Phase 2 で検討する
- **単独REGの判定には「REG成立時にチェリーを引いたか」の観察が必要**で、ぶどうより負荷が高い。
  実戦投入して、そもそも継続できる操作かを確認する(Phase 2 の最重要検証項目)
- 「差枚数」の表示仕様はホール・機種のデータカウンターによって異なるため、
  差枚が取得できないケースを許容する(`medals: number | null`)
- **マイジャグラーVI が2026年10月5日導入予定。** 導入後に調査JSONを追加し、
  `npm run machines` を再実行してマスタを更新する
- アイムジャグラーEX は2026年末前後に認定期限を迎える見込み。ネオアイムへの入替が進む
